import { NgModule } from '@angular/core';
import { AppTooltipDirective } from './app-tooltip.directive';

@NgModule({
  declarations: [AppTooltipDirective],
  exports: [AppTooltipDirective]
})

export class AppTooltipDirectiveModule {}
